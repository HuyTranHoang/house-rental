meta {
  name: Add Transaction
  type: http
  seq: 2
}

post {
  url: {{host}}/api/transaction
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
    "amount": 50000
  }
}
