meta {
  name: Add Amenity
  type: http
  seq: 4
}

post {
  url: {{host}}/api/amenity/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
      "name": "Full Noi That 123"
  }
}
