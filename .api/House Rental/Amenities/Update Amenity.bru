meta {
  name: Update Amenity
  type: http
  seq: 6
}

put {
  url: {{host}}/api/amenities/:id
  body: json
  auth: bearer
}

params:path {
  id: 1
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
      "name": "Full Noi That"
  }
}
