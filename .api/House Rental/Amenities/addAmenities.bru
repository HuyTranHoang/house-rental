meta {
  name: addAmenities
  type: http
  seq: 4
}

post {
  url: {{host}}/api/amenties/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
      "name": "Full Noi That"
  }
}
