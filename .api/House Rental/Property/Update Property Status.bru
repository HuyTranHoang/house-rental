meta {
  name: Update Property Status
  type: http
  seq: 11
}

put {
  url: {{host}}/api/properties/status/:id?status=REJECTED&reason=MỘT HAI BA BỐN
  body: json
  auth: bearer
}

params:query {
  status: REJECTED
  reason: MỘT HAI BA BỐN
}

params:path {
  id: 78
}

auth:bearer {
  token: {{jwt}}
}
