meta {
  name: Update Property Status
  type: http
  seq: 11
}

put {
  url: {{host}}/api/properties/status/:id?status=PENDING
  body: json
  auth: bearer
}

params:query {
  status: PENDING
}

params:path {
  id: 2
}

auth:bearer {
  token: {{jwt}}
}
